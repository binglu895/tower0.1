# 扑克塔防 (Poker Tower Defense) - 打包构建指南

本项目基于 `Vite` + `TypeScript` + `Canvas API` 构建，已完成移动端 H5 环境配置，完全兼容 TapTap 开发者后台的 Web 游戏上传标准。

## 🛠️ 1. 安装依赖环境

请确保你已经安装了 Node.js (推荐 v16+ 版本)。在项目根目录打开终端，执行：

```bash
npm install